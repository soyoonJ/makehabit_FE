import React from "react";
import styled from "styled-components";

import { history } from "../redux/configureStore";

const NotFound = (props) => {
  return <div>주소가 올바르지 않아요!</div>;
};

export default NotFound;
